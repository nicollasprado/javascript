// Teste logico ? se verdadeiro : se falso

let num = 10

res = (num%2 ? "par" : "impar")
console.log(res)

/*
0 = false
1 = true
*/

res2 = (num === "agua" ? "verdadeiro" : "falso")
console.log(res2)

// falso pois 10 não é string e não vale agua